{
  "name": "main",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "resolutions": {},
  "devDependencies": {
    "turbo": "2.0.10"
  },
  "packageManager": "yarn@1.22.22"
}
